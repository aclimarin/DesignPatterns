namespace src;

public class AudioCodec
{
    public void CodecSpecificOperation(string audioFile)
    {
        Console.WriteLine($"Converting audio file '{audioFile}' using the audio codec.");
    }
}