namespace src;

public class VideoCodec
{
    public void CodecSpecificOperation(string videoFile)
    {
        Console.WriteLine($"Converting video file '{videoFile}' using the video codec.");
    }
}