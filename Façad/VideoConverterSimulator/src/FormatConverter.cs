namespace src;

public class FormatConverter
{
    public void ConvertFormat(string videoFile, string targetFormat)
    {
        Console.WriteLine($"Converting video file '{videoFile}' to '{targetFormat}' format.");
    }
}